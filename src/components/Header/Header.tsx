import { HeaderContainer, SubtitleHeader, TitleHeader } from './style';

export default function Header() {
  return (
    <HeaderContainer>
     <TitleHeader>É pra já!</TitleHeader>
     <SubtitleHeader>Seu lanche em até 30min</SubtitleHeader>
    </HeaderContainer>
  );
}